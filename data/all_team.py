# < h3 >${teamInfo.name} < / h3 >
# < p > 리그: ${teamInfo.league | | '-'} < / p >
# < p > 연고지: ${teamInfo.location | | '-'} < / p >
# < p > 창단
# 연도: ${teamInfo.founded | | '-'} < / p >
# < p > 구장: ${teamInfo.stadium | | '-'} < / p >
# < p > 감독: ${teamInfo.coach | | '-'} < / p >
